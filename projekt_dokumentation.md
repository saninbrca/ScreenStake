# Projekt-Dokumentation — ARCHIVED / SUPERSEDED

> ⚠️ **This file is an outdated early-project snapshot and is NO LONGER maintained.**
> It described the app under its old working name ("Detox") and predates — among many other
> things — the rename to **Finite**, the removal of the incognito ban, the about:blank
> adult-block redirect, the ~133k-domain adult list, the `MONEY_FEATURES_ENABLED` build floor,
> the theme-token migration, SQLCipher encryption, and the server-side money authority work.
> Several of its "rules" now contradict the canonical invariants (e.g. it showed the forbidden
> inline `86400000` date calculation as correct).
>
> **Do not use this file as a reference. The canonical, maintained documentation lives in:**
> - [`CLAUDE.md`](CLAUDE.md) — always-loaded rules + doc index
> - [`docs/invariants.md`](docs/invariants.md) — the never-reverse rules (load FIRST)
> - [`docs/00_changelog.md`](docs/00_changelog.md) — chronological decisions & fixes
> - [`docs/01_architecture_and_stack.md`](docs/01_architecture_and_stack.md) … `docs/13_*` —
>   per-topic docs (see the index in `CLAUDE.md` §5)
>
> The file is kept (empty of content) only so old links/references don't dangle.
> Archived 2026-07-19; the last full version is available in git history
> (`git show 4b54701:projekt_dokumentation.md`).
